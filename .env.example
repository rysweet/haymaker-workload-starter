# Agent Haymaker workload credentials
# Copy to .env and fill in your values.
#
# The haymaker platform loads credentials from Key Vault in production.
# For local development, set environment variables directly.
# Load with: set -a && source .env && set +a

# --- Claude SDK (default) ---
# ANTHROPIC_API_KEY=sk-ant-...

# --- GitHub Copilot SDK ---
# GH_TOKEN=gho_...
# Or use: export GH_TOKEN=$(gh auth token)

# --- Microsoft SDK (Azure OpenAI with DefaultAzureCredential) ---
# AZURE_OPENAI_ENDPOINT=https://your-instance.openai.azure.com/
# AZURE_OPENAI_DEPLOYMENT=gpt-4o
# For local dev, run `az login` first. In Azure, managed identity handles auth.

# --- Mini SDK (litellm) ---
# Uses ANTHROPIC_API_KEY or OPENAI_API_KEY (whichever is set)
