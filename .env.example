# Agent Haymaker workload credentials
# Copy to .env and fill in your values.
#
# How credentials work:
#   The haymaker platform loads these from Key Vault in production.
#   For local development, the FilePlatform reads from environment variables.
#   Load your .env file before running: export $(cat .env | xargs)
#   Or use: set -a && source .env && set +a

# Optional: API key for external service integration
# MY_API_KEY=your-api-key-here

# Optional: LLM integration (if using agent-haymaker[llm])
# ANTHROPIC_API_KEY=sk-ant-...
# AZURE_OPENAI_API_KEY=...
# AZURE_OPENAI_ENDPOINT=https://your-instance.openai.azure.com/
